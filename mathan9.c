#include<stdio.h>
#include<conio.h>
int main()
{
 int l, r; 
 l = 0;
 r = arr_size-1; 
 while (l < r)
 {
 if(A[l] + A[r] == sum)
 return 1; 
 else if(A[l] + A[r] < s
 l++;
 else // A[i] + A[j] > sum
 }  
 return 0;
}
